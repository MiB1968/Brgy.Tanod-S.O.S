# Brgy. Tanod S.O.S — Security Fix Application Guide
Generated by GuardianForge · Smart Stabilizer God Mode
Date: 2026-06-06

---

## Overview

This batch contains **8 fixed files** addressing all Critical, High, and Medium
findings from the deep security review. Apply them in the order listed below.

Every file in this folder is a **complete replacement** for the corresponding
`src/` file in your real Acode project. Copy the entire file — do not patch
line-by-line unless stated otherwise.

---

## Pre-Flight Checklist

Before you start:

- [ ] Confirm `ENCRYPTION_KEY` is already set in your Cloud Run / deployment
      environment. If it is NOT set, do NOT apply the crypto.ts fix until you
      set it — otherwise all previously-encrypted resident PII will become
      unreadable. Generate a key first:
      ```
      node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
      ```
- [ ] Have your `.env` file open — you will be adding new variables.
- [ ] Confirm your Git working tree is clean before starting (so you can revert
      individual files if needed).

---

## Application Order

### Step 1 — crypto.ts  (CRIT-02 + CRIT-04)
**This file: `crypto.ts`**
**Destination in your project: `src/utils/crypto.ts`**

Fixes:
- `encryptField(null)` no longer crashes — returns null (matches test contract)
- Key is now read from `config.encryptionKey` (single source of truth)
- `decryptField` is null-safe and handles non-encrypted legacy rows gracefully
- JSON objects/arrays round-trip correctly

After applying: No other files need changing. The function signatures are
backward-compatible. All callers (`authController.ts`, `adminController.ts`,
`syncController.ts`) work without modification.

---

### Step 2 — auth.ts  (CRIT-01 + HIGH-01)
**This file: `auth.ts`**
**Destination in your project: `src/server/middleware/auth.ts`**

Fixes:
- Hardcoded `rubenlleg12@gmail.com` removed from server-side role elevation
- Firebase auth path now enforces `token_version` revocation (was skipped)
- MASTER_EMAILS env var is still supported for super_admin elevation

After applying: Test login with a normal account, a Google account, and confirm
that calling `revokeUserSessions(userId)` causes subsequent requests to return 401.

---

### Step 3 — socketAuth.ts  (HIGH-01 socket layer)
**This file: `socketAuth.ts`**
**Destination in your project: `src/server/middleware/socketAuth.ts`**

Fixes:
- Socket connections now check `token_version` and `status` for both JWT and
  Firebase auth paths
- Suspended users are rejected at connection time

After applying: Test a WebSocket connection with a suspended account.

---

### Step 4 — app.ts  (CRIT-03)
**This file: `app.ts`**
**Destination in your project: `src/server/app.ts`**

Fixes:
- CORS now enforces the `CORS_ORIGIN` env var in production
- Development mode still allows all origins for convenience
- Same-origin requests (Postman, server-to-server) always allowed

After applying:
1. Set `CORS_ORIGIN=https://your-frontend-domain.com` in your environment
2. Test that cross-origin requests from your frontend work
3. Verify that requests from an unlisted origin receive 403

---

### Step 5 — firestore.rules  (MED-01 + CRIT-01 Firestore layer)
**This file: `firestore.rules`**
**Destination in your project: `firestore.rules` (project root)**

Fixes:
- `isMasterAdminEmail()` function removed (was checking hardcoded email)
- `selfHealingSuperAdmin()` function removed (was allowing client-side role escalation)
- Owners can now update their profile but CANNOT change their role under any
  circumstances from the client. Role changes are Cloud Functions only.

After applying:
```bash
firebase deploy --only firestore:rules
```
Test that a user CANNOT write `role: 'super_admin'` to their own Firestore document.

---

### Step 6 — AuthContext.tsx  (CRIT-01 client-side)
**This file: `AuthContext.tsx`**
**Destination in your project: `src/context/AuthContext.tsx`**

Fixes:
- All 5 occurrences of the hardcoded email check removed
- `isInMasterEmailList()` helper centralizes the env-var check
- Self-healing super_admin logic retained but env-var-only

After applying: Rebuild the app and verify super_admin UI works for your
email address (which must be in `VITE_MASTER_EMAILS` in your `.env`).

---

### Step 7 — constants.patch.tsx  (CRIT-01 client-side / constants)
**This file: `constants.patch.tsx`**

This is a PATCH, not a full replacement. The `isRuben()` function in your
`src/constants.tsx` must be updated. Two options:

**Option A (recommended):** Replace just the `isRuben` block in your
`src/constants.tsx` with the `isMasterAdmin` export from this file.
The backward-compat alias (`export const isRuben = isMasterAdmin`) is included
so `NavigationSidebar.tsx` and `useAppLogic.ts` continue to work without changes.

**Option B:** Copy the full export block shown in this file into
`src/constants.tsx` replacing the existing `isRuben` function.

After applying: Rebuild and confirm the NavigationSidebar still shows the
super_admin panel for your email address.

---

### Step 8 — webhookRoutes.ts  (HIGH-02)
**This file: `webhookRoutes.ts`**
**Destination in your project: `src/server/routes/webhookRoutes.ts`**

Fixes:
- Telegram webhook validates `X-Telegram-Bot-Api-Secret-Token` header
- Constant-time comparison (timing-safe)
- If `TELEGRAM_WEBHOOK_SECRET` is not set → endpoint returns 403 (fail-safe)

After applying:
1. Generate a secret:
   ```
   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
   ```
2. Add to your environment: `TELEGRAM_WEBHOOK_SECRET=<generated value>`
3. Re-register your Telegram webhook:
   ```bash
   curl -X POST https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook \
     -H "Content-Type: application/json" \
     -d '{"url":"https://yourdomain.com/api/webhooks/telegram","secret_token":"<same value>"}'
   ```

---

### Step 9 — scrape.ts  (HIGH-03)
**This file: `scrape.ts`**
**Destination in your project: `src/server/routes/scrape.ts`**

Fixes:
- URL scheme validation (http/https only)
- Private IP / loopback blocking
- Restricted to tanod, admin, super_admin roles (residents excluded)

After applying: Verify scraping a public URL works. Verify that `http://localhost`
and `http://192.168.1.1` are rejected with 400.

---

### Step 10 — .env.example  (MED-02)
**This file: `.env.example`**
**Destination in your project: `.env.example` (project root)**

Adds the previously missing `API_KEY`, `API_KEY_ROLE`, and
`TELEGRAM_WEBHOOK_SECRET` variables, and improves documentation for all
existing variables.

---

## Post-Application Verification

After all files are applied and the app rebuilds:

- [ ] Normal user login works (JWT path)
- [ ] Google Sign-In works (Firebase path)
- [ ] Resident registration with empty medical fields succeeds (was crashing)
- [ ] Resident registration with medical data encrypts correctly
- [ ] CORS allows your frontend domain, blocks others
- [ ] Revoking a user session causes their next request to return 401
- [ ] Telegram webhook returns 403 without the correct secret header
- [ ] Scraping `http://localhost` returns 400
- [ ] Firestore: user cannot write `role: super_admin` to their own document

---

## Environment Variables to Add/Set

```
ENCRYPTION_KEY=<32-byte hex — generate before first run>
JWT_SECRET=<32-byte hex>
API_KEY=<32-byte hex>
API_KEY_ROLE=tanod
CORS_ORIGIN=https://your-frontend.com
VITE_MASTER_EMAILS=your@email.com
TELEGRAM_WEBHOOK_SECRET=<32-byte hex — then re-register webhook>
```
